# Only needed to preview the site on your own machine:
#   bundle install
#   bundle exec jekyll serve
# GitHub Pages ignores this file and builds the site by itself.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
